<?xml version="1.0" encoding="utf-8"?>
<queries>
	<!-- Consulta para obter todos os produtos -->
	<query name="GetAllProducts">
		SELECT * FROM Produtos;
	</query>

	<!-- Consulta para obter um produto por ID -->
	<query name="GetProductById">
		SELECT Name, ImageUrl, Price, Description, Id, ReleaseDate
		FROM Products
		WHERE Id = @Id;
	</query>

	<!-- Query para adicionar um produto -->
	<query name="AddProduct">
		INSERT INTO Produtos (Name, ImageUrl, Price, Description, Id, ReleaseDate)
		VALUES (@Name, @ImageUrl, @Price, @Description, @Id, @ReleaseDate)
	</query>

	<!-- Query para remover um produto usando id-->
	<query name="RemoveProductById">
		DELETE FROM Produtos
		WHERE Id = @Id;
	</query>



	<!-- Consulta para deletar um usuário -->
	<query name="DeleteUsuario">
		DELETE FROM Usuarios
		WHERE usuarioid = @Id;
	</query>
</queries>
