<?xml version="1.0" encoding="utf-8"?>
<queries>
	<query name="GetAllDiretores">
		SELECT 
			nome,
			descricao,
			imagem_url as ImagemUrl,
			usuario_id as UsuarioId,
			diretoria_id as DiretoriaId,
			email,
			github_link as GithubLink,
			linkedin_link as LinkedinLink
		FROM diretores
	</query>
	<query name="CreateDiretor">
		INSERT INTO diretores
			(nome,
			descricao,
			usuario_id,
			diretoria_id,
			email,
			github_link,
			linkedin_link)
		VALUES (@Nome, @Descricao,@UsuarioId,@DiretoriaId, @Email, @GithubLink, @LinkedinLink)
	</query>>
	<query name="GetDiretorById">
		SELECT
			nome,
			descricao,
			usuario_id as UsuarioId,
			diretoria_id as DiretoriaId,
			email,
			github_link as GithubLink,
			linkedin_link as LinkedinLink
		FROM diretores 
		WHERE id=@id
	</query>
	<query name="DeleteDiretor">
		DELETE FROM diretores WHERE id=@id
	</query>
	<query name="UpdateDiretor">
		UPDATE diretores
			SET nome = @Nome,
				descricao = @Descricao,
				imagem_url = @ImagemUrl,
				diretoria_id = @DiretoriaId,
				email = @Email,
				github_link = @GithubLink
				linkedin_link = @LinkedinLink
				WHERE id=@id
	</query>
</queries>